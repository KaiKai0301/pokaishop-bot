services:
  - type: web
    name: pokaishop-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: TOKEN
        sync: false
      - key: SPREADSHEET_ID
        sync: false
      - key: ADMIN_IDS
        sync: false